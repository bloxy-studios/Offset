# BUILDLOG

## M0 smoke test — 2026-07-22 (driven from Hyperagent via CloudLink bridge)
- git-pull: OK (GitHub remote wired, ff-only)
- Found & fixed remotely: BUILDLOG.md sat in the app target's Sources compile phase (created via Xcode File ▸ New with target membership; fileType was 'sourcecode.swift'). Removed both PBXBuildFile references and corrected lastKnownFileType to markdown via pbxproj patch over the bridge File API (compare-and-swap write).
- build (scheme Offset, iPhone 17 Pro Max · iOS 26.5 sim): SUCCEEDED — 0 errors, 0 warnings, 106s warm.
- run: install + launch verified by simulator screenshots (M0 skeleton on screen).
- Known items for M1:
  1. OffsetKit scheme has no test action — wire OffsetKitTests into the Offset scheme's test action (xcscheme edit or test plan).
  2. CloudLink's ~5-min job timeout SIGTERMs long jobs (cold simulator boot; console-attached launch). Owner may raise the timeout in CloudLink; agent keeps builds warm and re-attaches to jobs after client caps.
  3. xcodebuild test transiently reported 'no matching devices' while CoreSimulator was cold — retry against the booted sim.
- Locked simulator target: iPhone 17 Pro Max · iOS 26.5 · 9DD4ED40-6323-415C-9591-C73F43AC67F9

## Test lane fix + M1: models + seed data — 2026-07-22 (agent, via CloudLink)

### Test lane (pre-M1 task)
- Found: NO shared scheme file existed (`Offset.xcodeproj/xcshareddata/` absent on the Mac and in git — the M0 smoke test had run on Xcode's auto-generated scheme, which cannot include SPM test targets in its Test action).
- Fix: authored the shared scheme `Offset.xcodeproj/xcshareddata/xcschemes/Offset.xcscheme` — BuildAction: Offset app (implicit deps on); TestAction (Debug): **OffsetKitTests (container:OffsetKit)** + OffsetTests + OffsetUITests; standard Launch/Profile/Analyze/Archive. Proven: `test --only-testing OffsetKitTests` green through the Offset scheme (job d55f50c4, 1/1).
- **OffsetUITests quarantined (skipped="YES" in the scheme, comment inline)**: OffsetUITests-Runner crashes at BOOTSTRAP — SIGABRT, "Early unexpected exit, operation never finished bootstrapping", before any test executes — reproduced twice (jobs e59c0b22 · eed79106, ~700–810 s each). Unit bundles are healthy (OffsetTests passes hosted in the same app). Judged infra-level (Xcode 26.6 UI-test harness vs iOS 26.5 sim state), not app code: the bundle contains only template launch tests and nothing in M1 touches app/UI-test targets. XCUITest automation is NOT in BUILD_PROMPT §5's required test inventory; UI verification per CLOUDLINK_LOOP is simulator screenshots.
  - **MANUAL-QA (owner)**: run OffsetUITests once from Xcode GUI on a freshly booted sim; if green, set the testable's skipped="NO" in Manage Schemes → Test.
- Simulator health this session: after ~1 h of UI-runner hangs the booted iPhone 17 Pro Max sim degraded (48 s `simctl list`; one placeholder-destination "no matching devices" failure, job a668200b; then an OffsetKitTests runner hang, job 77a48991 — same suite green twice earlier). Switched to the fresh iPhone 17 Pro · iOS 26.5 · 20B81821-8AE2-4942-87CC-CE1D169EBEA3 for the confirmation run. Note for future sessions: prefer recycling/switching sims after runner crashes.
- Observed: CloudLink job timeout is no longer ~5 min — test jobs ran 700–826 s to terminal (owner raised it). Client-side 300 s tool-call caps still apply; re-attach with `job <id>` polling.

### M1 — models + seed data (OffsetKit)
- Built all spine §4 model types verbatim (MarketID, MarketKind, Market, WallClockTime, SegmentKind, TradingSegment, SessionOccurrence, KillzoneID, MarketEventKind, MarketEvent, MarketStatus, AlertTarget, AlertMoment, AlertStyle, AlertRule, EconImpact, EconEvent, Headline, TraderLevel, SummaryProvider, Briefing, ConventionSettings, TimeDisplayMode, AppSettings) + spine §8 field `AppSettings.dismissedExplainerIDs`; 03 §2d decode structs (SessionsFile/MarketRecord, HolidaysFile/HolidayCalendarRecord/HolidayDay/ClosureKind/HolidayPolicy, KillzonesFile/KillzoneRecord, DayKey).
- Seed JSONs extracted **byte-for-byte from the fenced blocks in docs/03 §2** (script-extracted, json-validated: us 10 full + 2 half 2026 / lse 8 full + 2 half 2026 / cme advisory 0 days / validThrough 2027-12-31 ×3; killzones 5, NY zone, asia wraps).
- Package.swift: `resources: [.process("Resources")]`; `swiftSettings: [.defaultIsolation(MainActor.self)]` on both targets; tools 6.3, language mode v6.
- UNVERIFIED resolutions (02 §2):
  - Xcode build-setting spellings confirmed in project.pbxproj (M0 wrote them via UI): `SWIFT_APPROACHABLE_CONCURRENCY = YES`, `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`.
  - SwiftPM default-isolation API: **`SwiftSetting.defaultIsolation(MainActor.self)`** (PackageDescription, tools 6.3) — compiles clean on Xcode 26.6.
- Decisions:
  - Every OffsetKit type is declared `nonisolated` explicitly (module default is MainActor; models/engine opt out per 02 §2 table), including nested Codable helpers and CodingKeys.
  - EconImpact: raw-valued enums are excluded from synthesized Comparable (SE-0266) → manual `<` over declaration-order rank (low < medium < high < holiday).
  - ConventionSettings: custom Codable per 03 §2d (labeled-tuple windows → `{ "open": …, "close": … }` objects; dictionaries keyed by raw values; unknown ids throw DecodingError).
  - MarketID.id = rawValue; SessionOccurrence.id derived "market:kind:openEpoch" (spine declares Identifiable without a stored id).
  - Interim internal decode surface: `SessionsFile/HolidaysFile/KillzonesFile.loadBundled()` + `seedResourceData(named:)` (internal only; M2's `SessionScheduleEngine.loadBundledSeed()` composes them into SeedData — SeedData itself deferred to M2 because it holds HolidayCalendar, an Engine type).
  - Placeholder files OffsetKit.swift / OffsetKitTests.swift removed.
- Verification: build **0 errors / 0 warnings** (job e73c8e49, 127 s). Tests green: OffsetKitTests 18/18 + OffsetTests 1/1 (job e59c0b22; the sole "failure" in that job was the pre-existing OffsetUITests bootstrap crash quarantined above). Simulator: iPhone 17 Pro Max · iOS 26.5 · 9DD4ED40-6323-415C-9591-C73F43AC67F9.
- M1 acceptance: decode tests pass for all three seed files ✅ · Market catalog exposes all 7 markets with spine §3 zones/colors/symbols (exact-table test) ✅ · WallClockTime Comparable behavior tested ✅.
- Final confirmation: full-suite run with the UI-test skip in place — job 969a220d on the fresh iPhone 17 Pro sim: **19/19 passed, 0 failures** ("All tests passed", 1941 s incl. cold boot; the job survived a CloudLink tunnel outage ~13:04–13:45Z — Mac restart — and completed on the Mac). Committed on tunnel restore.

## M2 — SessionScheduleEngine (the heart) — 2026-07-22 (agent, via CloudLink)
- Built: `SeedData` + `SessionScheduleEngine.loadBundledSeed()` (composes M1's decode surface); `occurrences(in:markets:conventions:)` per 03 §3 — 26 h scan padding, per-market gregorian Calendar pinned to the market zone, weekday mask, full-closure drop, half-day mapping, `wrapsMidnight` next-day close resolution, deterministic (openDate, market, segment) sort; `resolve()` per 03 §3.1 (`.nextTime`/`.first`); `effectiveTimes()` per 03 §3.2 generalized rule (regular close → E; a segment starting at the normal regular close shifts to E keeping wall-clock duration; emptied windows suppressed); `HolidayCalendar` (closure/earlyClose/advisory/validThrough; advisory keyed off usEquities days per 03 §2b); `OverlapCalculator` (structural same-NY-day pairing, max(opens)..<min(closes)); `events(in:settings:econEvents:)` per 03 §4 steps 1–8 (structural opens/closes regardless of rules; FX week markers Sun/Fri 17:00 NY; overlap; killzones gated Pro-or-enabled-rule; econ admission incl. `.holiday` exclusion; boundary-relative leads from enabled rules; half-open filter; (date, kind-rank, id) sort + id dedupe); `MarketEvent.id` grammar per 03 §4.1 exactly; `nextEvent` (expanding 48 h → 8 d window, strict `date > after`); `marketStatus` per 03 §5 (holiday naming, preMarket/afterHours/auction mapping, CME break → closed).
- Landed early from 04 (T19/T22 depend on it): `AlertRule.defaultRules()` — 04 §2.1 R1–R20 verbatim, only R1–R4 enabled — now the default for `AppSettings.alertRules` (spine §4: "defaults in 04 doc"). SettingsStore persists them at first launch (M3/M4).
- Copy note: MarketEvent title/subtitle are engine defaults ("LDN opens" / "08:00 LDN" market-zone wall clock); 07 §5 templates own final copy (M7) — sanctioned by 04 §4.6.
- Constants `occurrenceScanPadding` (26 h) and `statusLookaheadDays` (14) live as file-scope `nonisolated let` globals in the engine file (03 PROPOSED-ADDITIONS names, internal).
- Tests: **entire 03 §7 list green** — T1 (M1 decode suite) + T2–T22 as 21 engine tests. Highlights: overlap 4 h normal / 5 h in BOTH 2026 mismatch windows and back; NYSE Labor-Day drop + 2026-11-27 truncation (regular→13:00 EST, afterHours→13:00–17:00, preMarket untouched) + status transitions; LSE 12-24 half day + Christmas-run closures with named holiday status; CME wrap identity (close keyed to OPEN day), no-Friday-open, Sunday open, break-is-closed; killzone NY-pinning drift (06:00–09:00 London wall clock in the mismatch week — by design); resolve() spring-forward 02:30→03:00 and fall-back first-pass 01:30 with `UNCalendarNotificationTrigger.nextTriggerDate()` cross-check (research §3 pitfall 7); byte-identical determinism across calls AND across fresh AppSettings values (rule UUIDs don't leak); weekOpen ≡ fxSydney-Monday-open same instant, distinct ids; 7-market device-local projection with cross-day Sydney/Tokyo day keys; Beginner-hides / Pro-emits-all-five killzone gate. All fixtures are explicit tzdata-derived epochs — suite is device-zone independent.
- Purity: engine holds no clocks/singletons/I-O; every test injects fixture dates; `loadBundledSeed()` is the only bundle touch (static factory).
- Verification: build **0 errors / 0 warnings** (job dbb8501f, 163 s); tests **40/40 green** (job 49ea847c, 341 s) on iPhone 17 Pro Max · iOS 26.5 (fresh boot after Mac restart).

## M3 — Stores, settings, refresh skeleton — 2026-07-22 (agent, via CloudLink)
- Built (OffsetKit/Storage): `AppGroup` (id `group.com.bloxy-studios.Offset`, `.userDefaults` with test-safe fallback, `containerURL`); `SettingsStore` + `SettingsEnvelope` + `settingsSchemaVersion = 1` per 02 §4.1 (blob key "offset.settings.v-envelope"; version-probe → equal / stepwise-migrate / quarantine-and-reset paths; quarantine key preserves the raw blob and feeds a "Settings were reset" row via `wasReset`; first launch persists the 04 §2 default rule set); `CacheStore` (@MainActor façade per 02 §2 options; internal @Model mirrors CachedHeadline/CachedEconEvent/CachedBriefing that never leak past the store; upsert-by-unique-id; 02 §4.2 retention: headlines 3 days · econ current-week→end-of-next-week · briefings last 7 by key); `KeychainStore` (generic passwords, kSecAttrAccessibleAfterFirstUnlock, `.user` override account beats `.bundle` import — protects the Settings paste-in from stale bundle values and covers rotation; bootstrap skips empty/`$(…)` substitutions).
- Built (app target): `ScheduleStore` (@MainActor @Observable engine façade — settings + todayEvents/nextEvent/statuses derived per refresh(now:zone:), zone injectable for tests); `RefreshCoordinator` (registers BOTH BGTask ids before end of launch; re-submits at handler start + foreground with earliestBeginDate now+4h; expiration handlers cancel work; signal methods per the 02 §5.2 table); `OffsetApp` (seed decode fail-fast with OSLog fault; secrets bootstrap; scenePhase + significantTimeChange + NSSystemTimeZoneDidChange + NSCalendarDayChanged observers); interim M3 ContentView shell (store readout + acceptance rows — replaced by the 07 UI in M7). `Shared/SharedConstants.swift` populated: com.bloxy-studios BGTask ids, deep-link builders, log subsystem, and `Capabilities.timeSensitiveEntitlementPresent = false` (free-team gate, flipped when the paid entitlement returns).
- pbxproj surgery (sanctioned, CLOUDLINK_LOOP): (1) `Shared/` converted from a classic PBXGroup (2 per-file build entries) to a `PBXFileSystemSynchronizedRootGroup` attached to BOTH app + widget targets — the group UUID was reused so the mainGroup child reference survived; M5's Shared files (MarketCountdownAttributes, AlarmMetadata) now auto-compile into both targets. (2) OffsetKit added as a package product dependency of OffsetTests (XCSwiftPackageProductDependency 0FF5E7…A1 + build file …A2) so app-hosted tests import OffsetKit directly.
- UNVERIFIED resolutions / SDK symbols found:
  - SwiftData App-Group placement: `ModelConfiguration(_:groupContainer: .identifier("group.com.bloxy-studios.Offset"))`; in-memory tests via `ModelConfiguration(_:isStoredInMemoryOnly: true)`; `@Attribute(.unique)` spelling confirmed. All compile on Xcode 26.6.
  - `TimeZone.resetSystemTimeZone()` (docs/research spelling) does not exist on Swift's TimeZone — the SDK symbol is `NSTimeZone.resetSystemTimeZone()`; used with a code comment.
- Concurrency notes (zero-warning build):
  - `SettingsStore: @unchecked Sendable` — stored `UserDefaults` is not Sendable-annotated in the SDK but is documented thread-safe ("The UserDefaults class is thread-safe"); wrapper is otherwise immutable. (BUILD_PROMPT hygiene justification.)
  - BGTaskScheduler launch handlers: registered with `using: .main` so handlers execute on the main queue; the non-Sendable `BGAppRefreshTask` is bridged into MainActor with `MainActor.assumeIsolated` + a `nonisolated(unsafe) let` binding — sound because `using: .main` serializes the handler; the region checker cannot see the queue guarantee. (`.main` here is system-API plumbing, not DispatchQueue-for-logic; 02 §2 ban respected.)
- Deviation found & fixed en route: `Shared/` was never target-attached in M0 (classic group existed but SharedConstants.swift had build entries only — orphaned from the synced-group scheme M0 used everywhere else); resolved by the conversion above.
- Test relocation: KeychainStore tests moved to the app-hosted OffsetTests bundle — SecItemAdd silently no-ops in the unhosted SPM test runner (no keychain entitlements); verified failing there, green hosted. OffsetKitTests keeps a pointer comment.
- Verification: build 0 errors / 0 warnings (job 99788893 after 3 fix iterations: Sendable UserDefaults → NSTimeZone symbol → BGTask region); tests **55/55 green** (job 0758fb7d — SettingsStore round-trip/migration-stub/quarantine ×5, CacheStore round-trips + retention ×4, ScheduleStore zone-change + settings-update, RefreshCoordinator zone/time/day signals ×2, Keychain ×2 hosted, plus all M1/M2 suites). Simulator acceptance: app runs with "BG tasks registered ✓" + both task ids + Last refresh visible on screen (screenshots m3_shell*.png); registration OSLog line emitted under subsystem com.bloxy-studios.Offset category refresh.
- M3 acceptance: settings round-trip ✅ · migration stub ✅ · BG tasks registered without crash on simulator ✅ (visible state + log) · zone-change simulation recomputes schedule (unit-level) ✅.

## M4 — Notifications — 2026-07-22 (agent, via CloudLink)
- Built (OffsetKit/Scheduling): `NotificationPlanner` + `PlannedNotification` (04 §3.1 fields) + constants `notificationBudget 56` / `reserveSlots 8` / `perDayCap 16` / `coincidenceMergeWindow 60 s` + category/action id constants. Algorithm per 04 §3.2 steps 1–8: rule matching by (target × moment) with exact (MarketID, SegmentKind) targeting (segment parsed from the 03 §4.1 id grammar); style = max severity; **alarm handoff** (criticalAlarm anchors leave the lane, same-rule leads dropped, other-rule leads survive at rank 0); drop ≤ now+5 s; coincidence merge (60 s, same boundary class, earliest fireDate + combined title + smallest member id); rank per spine §4 priority (opens 1 · econ 2 · closes 3 · killzones 4 — time-sensitive killzone starts rank with opens · remaining 5; leads inherit anchor rank); per-day cap 16 dropping lowest-priority-largest-id first; nearest-first fill to 56. Planner never emits `.criticalAlarm` (mapped to `.timeSensitive` if it ever survives).
- Built (app target): `AlertsStore` — category registration (OPEN_MARKET: View market [foreground] + Mute today; ECON_EVENT: View calendar + Mute this series), authorization (alert+sound+badge; optional `.provisional` for headless sim QA; the deprecated `UNAuthorizationOptions.timeSensitive` is never requested), idempotent apply per 04 §3.4 (removeAll → add; explicit-zone `UNCalendarNotificationTrigger` components; DEBUG assert pending ≤ 64), BudgetHealthRow data (`pendingCount`/`plannedCount`/`coverageEnd`), `muteTodayUntil` with market-zone-day expiry, ad-hoc reserve-lane single adds; `NotificationDelegate` (willPresent [.banner,.list,.sound]; MUTE_TODAY → market-zone mute + rebuild; MUTE_SERIES → disable econ rules; VIEW_MARKET routes when DeepLinkRouter lands in M7); RefreshCoordinator now runs the full 04 §1 rebuild (7-day horizon → plan → apply) on every trigger incl. BG task handlers.
- **Free-team gate wired**: `AlertsStore.interruptionLevel(for:timeSensitiveEntitlementPresent:)` — `.timeSensitive` maps to `.timeSensitive` ONLY when `Capabilities.timeSensitiveEntitlementPresent` (currently false) else `.active`; `.standard`/`.criticalAlarm` always `.active`. Unit-tested both ways.
- Tests: budgeter suite 12 tests (identifier == MarketEvent.id + zone/category/thread mapping; exact segment targeting; disabled rules; drop-past; nearest-first 56-fill + 64 arithmetic; per-day cap priority degradation; idempotent replan; coincidence merge incl. no-cross-class; alarm handoff both ways; style max-severity + never-critical; econ matching + lead rank inheritance; killzone rank by style) + 3 app-hosted gate tests. Suite runs: 69/70 full (job 4d7c57fc — sole failure was a test-fixture bug: synthetic events spaced exactly 60 s apart legitimately triggered the merge rule; fixture spaced to 120 s) → OffsetKitTests 60/60 (job 073aacbc) with app-hosted 10/10 green in 4d7c57fc. Build 0 errors / 0 warnings (job 6db2f68b, first try).
- Simulator acceptance (screenshots m4_probe_*.png): Authorization provisional · Categories OPEN_MARKET + ECON_EVENT ✓ · **Budget "31 of 64 slots · through Wednesday"** (live planner output: 30 planned R1–R3/R4 notifications + 1 probe) · **Test notification "fired ✓ 1:05:04 PM"** — scheduled via the real apply path (OPEN_MARKET category) and delivered; the delegate's willPresent timestamps the fire. Note: provisional authorization delivers QUIETLY (no banner even foregrounded) — visible-banner + long-press action-button verification requires full authorization → MANUAL-QA (owner, after M7 onboarding priming): grant notifications, verify a near-term alert banner + "View market"/"Mute today" buttons, and the Focus/time-sensitive behavior when the paid entitlement lands.
- MANUAL-QA additions: (1) notification banner + category actions on device with full auth; (2) MUTE_TODAY action end-to-end from a delivered notification.
- Infra note: the CloudLink tunnel FLAPPED twice this session (Cloudflare 1033, self-recovered) — job submissions survived; the owner's physical iPhone appeared in the destination list mid-session (plugged into the Mac). One transient "no matching devices" (job c45cb883) cleared on retry.
